// Fill these in from your Supabase project (Settings > API), then reload the app.
// Leave SUPABASE_URL empty to run fully offline/local with no sync.
window.SANKALP_CONFIG = {
  SUPABASE_URL: '',
  SUPABASE_ANON_KEY: ''
};
